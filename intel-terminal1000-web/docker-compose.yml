version: '3.8'
services:
	frontend:
		build: .
		container_name: intel-terminal1000-web-frontend
		ports:
			- "8081:80"
		restart: unless-stopped
		environment:
			- TZ=UTC
		networks:
			- webnet
networks:
	webnet:
		driver: bridge