# Intel Terminal 1000 Web

This is the web-sanitized, public-facing version of Intel Terminal 1000. It includes only the dashboard and map, with no settings or admin features exposed.

- For the full-featured version (admin, settings, source management), see `dev-intel-terminal-1000`.
- For deployment, see the included Dockerfile and instructions.
